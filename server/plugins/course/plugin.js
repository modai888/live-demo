'use strict';


module.exports = {
    name: 'course',
    version: '',

    register(server, options) {
        // 注册路由
        return [
            // {
            //     path: '/:id/live',
            //     get: (ctx) => { ctx.body = 'get live' }
            // },
            // {
            //     path: '/:id',
            //     get: (ctx) => { ctx.body = 'get course' }
            // }
        ]
    }
}